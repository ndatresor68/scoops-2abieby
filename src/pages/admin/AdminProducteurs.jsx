import Producteurs from "../../Producteurs"

export default function AdminProducteurs() {
  // Reuse the existing Producteurs component with admin privileges
  // The component already has full CRUD capabilities
  return <Producteurs />
}
